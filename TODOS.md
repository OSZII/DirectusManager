Add a version controll view for the schemas:
After current Task modify the git integration to only show schema changes and when pushing only push the changes of the Schema and not the entire project. Add another Modal to provide a git commit message. And on every git commit prepend message [DirectusManager Schema Push]
